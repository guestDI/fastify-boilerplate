const { test } = require('tap');
const aboutService = require('../../src/services/aboutService');

test('aboutService: should fetch about information', async (t) => {
    // Placeholder: mock data and test logic
    t.pass('about fetched correctly');
});
