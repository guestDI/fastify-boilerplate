const { test } = require('tap');
const aboutController = require('../../src/controllers/aboutController');

test('aboutController: should return data correctly', async (t) => {
    // Placeholder: mock data and test logic
    t.pass('user data is returned correctly');
});
